# Back end (Functions)
NEON_DATABASE_URL=postgres://USER:PASSWORD@HOST/dbname
VOYAGE_API_KEY=...
VOYAGE_EMBED_MODEL=voyage-3.5
VOYAGE_RERANK_MODEL=rerank-2.5
EMBED_DIM=1024
PUBLIC_BASE_URL=http://localhost:8888

# Front end (Vite-visible -> must start with VITE_)
VITE_APP_NAME=Gift of Time Assistant
VITE_API_BASE=/    # keep "/" (same origin via Netlify Dev proxy) or full origin for external API
